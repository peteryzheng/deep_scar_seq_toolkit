"""Package defaults.

Keep defaults here so CLIs and library calls share the same baseline behavior.
"""

DEFAULT_WINDOW = 5
DEFAULT_PYSAM_QUIET = False
DEFAULT_INCLUDE_MATE = False
DEFAULT_UMI_TAG_PRIORITY = ("RX", "UR")
